typestate Iterator {
  Initialize = {
    Iterator(int[]): HasNext
  }
  HasNext = {
    boolean hasNext(): <true: Next, false: end>,
    drop: end
  }
  Next = {
    int next(): HasNext,
    drop: end
  }
}
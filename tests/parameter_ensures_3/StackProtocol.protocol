typestate StackProtocol {
  Init = {
    boolean isEmpty(): <true: Init, false: CanPop>,
    boolean isFull(): <true: Init, false: CanPush>,
    void close(): end
  }
  CanPush = {
    boolean isEmpty(): <true: Init, false: CanPop>,
    boolean isFull(): <true: Init, false: CanPush>,
    void push(int): Init,
    void close(): end
  }
  CanPop = {
    boolean isFull(): <true: Init, false: CanPush>,
    boolean isEmpty(): <true: Init, false: CanPop>,
    int pop(): Init,
    void close(): end
  }
}
